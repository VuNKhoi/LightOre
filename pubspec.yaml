name: lightore
description: A Flutter app inspired by Boeing ForeFlight.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  go_router: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
